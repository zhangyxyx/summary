闭包：函数a的内部函数b被函数a外的一个变量引用的时候，就创建了一个b闭包
function a(){
	var i=0;
	function b(){
		alert(i);
	}
	return b;
}
var c=a();
c();
闭包应用的场景：
1 使用闭包代替全局变量
2 函数外或在其他函数中访问某一函数内部的参数
3 在函数执行之前为要执行的函数提供具体函数
4 在函数执行之前为函数提供只有函数执行或引用时才能知道的具体函数
5 为节点循环绑定click事件，在事件函数中使用当次循环的值或节点，而不是最后一次循环的值或节点
6 暂停执行
7 包装相关功能